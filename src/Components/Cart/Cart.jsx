import './Cart.css';

const Cart = ({cartItems, addToCart, removeFromCart}) => {

    const totalPrice = cartItems.reduce();
  return (
    <div>
      Cart away
    </div>
  )
}

export default Cart
